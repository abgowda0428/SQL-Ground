USE TEST;

GO

SELECT TOP 5
experience
FROM employees
ORDER BY experience ASC;

-- TOP DO NOT DEPENDS ON ANY COL.. ,AND IT SHOULD BE SPECIFIED AFTER SELECT.
-- USE ORDER BY TO GET THE TOP A MEANING FULL,IF NOT RANDOM IT WILL SELECT ANY TOP COL SPECIFIED.

-- STATIC COLOUM

SELECT
emp_id,
emp_name,
emp_id * 5 AS 'ATTENDENCE', -- FIRST WE SHOULD DEFINE THE DATA THEN THE ALIES FOR THAT DATA,
'BANGLORE' AS 'STUDENT LOCATION' -- WE CAN HAVE CONSTANT VALUE ALSO IN STATIC VALUE.
FROM                       
employees

-- CREATING THE TABLE

CREATE TABLE AUTOCRAT(
ID INT NOT NULL,
EMP_NAME VARCHAR(50),
SALARY BIGINT DEFAULT 150000,
ADDRESS TEXT NOT NULL,
CONSTRAINT PK_AUTOCRAT PRIMARY KEY(ID)
);

GO

-- SHOW ALL TABLE IN DATABASE
SELECT * 
FROM sys.tables;
-- OR
SELECT name 
FROM sys.tables;
-- Show all tables + schema
SELECT schema_name(schema_id) AS SchemaName,
       name AS TableName
FROM sys.tables;
-- Show table columns (structure of a table)
EXEC sp_columns Students;

-- INSERTING THE DATA INTO TABLE
INSERT INTO AUTOCRAT (ID,EMP_NAME,ADDRESS)
VALUES
(1, 'Rahul Sharma','Bangalore'),
(2, 'Sneha Reddy','Hyderabad');

SELECT * FROM AUTOCRAT;